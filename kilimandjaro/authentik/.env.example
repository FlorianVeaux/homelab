# Authentik Environment Variables
# Copy this file to .env and fill in the values

# Authentik version to use (must be 2025.1 or later for passkey autofill/conditional UI)
# Check releases at: https://github.com/goauthentik/authentik/releases
# Examples: 2025.1, 2025.2, latest
AUTHENTIK_VERSION=2025.12.1

# Tailscale auth key for ts-authentik container
# Generate at: https://login.tailscale.com/admin/settings/keys
# Use a reusable key with tag:container
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxx

# PostgreSQL password (generated with: openssl rand -base64 32)
AUTHENTIK_PG_PASSWORD=xxxxx

# Authentik secret key for session signing (generated with: openssl rand -base64 60)
AUTHENTIK_SECRET_KEY=xxxxx

# NOTE: Authentik public URL is configured in the UI (System → Settings), not via environment variable
# Set it to: https://auth.florianveaux.fr
