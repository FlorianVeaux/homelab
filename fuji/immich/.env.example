# Immich Environment Variables
# Copy this file to .env and fill in the values

###########################################
# Docker Image Version
###########################################

# Immich version to use (default: 'release' for latest stable)
# You can pin to a specific version like 'v1.117.0' to prevent automatic updates
# Check releases at: https://github.com/immich-app/immich/releases
IMMICH_VERSION=release

###########################################
# Storage Locations
###########################################

# Upload location - where your photos and videos are stored
# This should be on a large drive with plenty of space
# Example: /mnt/media/immich/upload or /nvme/docker/immich/upload
UPLOAD_LOCATION=/volume1/immich/immich_files

# Thumbnail location - where generated thumbnails are stored
# Can be on a faster SSD for better performance
# Example: /nvme/docker/immich/thumbs
THUMB_LOCATION=/volume2/docker/immich/thumbnails

###########################################
# Database Configuration
###########################################

# PostgreSQL database location - where the database files are stored
# Should be on an SSD for best performance
# Example: /nvme/docker/immich/postgres
DB_DATA_LOCATION=/volume2/docker/immich/postgres

# PostgreSQL database name
DB_DATABASE_NAME=immich

# PostgreSQL username
DB_USERNAME=immich

# PostgreSQL password (generate with: openssl rand -base64 32)
DB_PASSWORD=ChangeMe_GenerateSecurePassword

###########################################
# Notes
###########################################

# For Immich application configuration (OAuth, public URL, etc.),
# see the stack.env file which contains runtime configuration for
# the Immich server itself.

# This .env file is only for Docker Compose infrastructure variables
# (paths, database credentials, version pinning).
